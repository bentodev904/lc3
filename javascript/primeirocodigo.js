function compare(x, y) {
    alert("-" + x + "-");
    alert("-" + y + "-");
    soma = x + y;
    mult = x*y;
    div = x/y;
    document.write("Soma = " +soma +'' +"Multiplicacao = "+mult +'' +"Divisao = "+div +'')
}